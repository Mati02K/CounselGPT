apiVersion: v1
kind: Service
metadata:
  name: dcgm-exporter
  namespace: cse239fall2025
  labels:
    app: dcgm-exporter
spec:
  selector:
    app: dcgm-exporter
  ports:
  - name: http
    port: 9400
    targetPort: 9400
