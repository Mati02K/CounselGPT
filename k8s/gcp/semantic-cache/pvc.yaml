apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-cache-data
  labels:
    app: semantic-cache
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi  # 10GB for cache data
  storageClassName: standard-rwo

