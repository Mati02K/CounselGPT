# Backend API URL (base URL without /infer)
# For local development: http://localhost:8000
# For production: http://your-backend-url
VITE_API_URL=http://localhost:8000

# Model Configuration (JSON format)
# Each model should have an 'id' and 'name'
VITE_MODELS=[{"id":"qwen","name":"Qwen2.5-7B Legal (LoRA)"},{"id":"llama","name":"Llama-2-7B"}]

# Default selected model
VITE_DEFAULT_MODEL=qwen
